<%= form_for(@feed_url) do |f| %>
  <% if @feed_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@feed_url.errors.count, "error") %> prohibited this feed_url from being saved:</h2>

      <ul>
      <% @feed_url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url_name %><br />
    <%= f.text_field :url_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
